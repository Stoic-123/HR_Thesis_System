node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Database seed — contains real data, generate fresh with: mysqldump -u root hrms > prisma/seed.sql
prisma/seed.sql
